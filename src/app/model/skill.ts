export class Skill {
  id: number;
  nombre: string;
  porcentaje: string;

  constructor(id: number, nombre: string, porcentaje: string) {
      this.id = id;
      this.nombre = nombre;
      this.porcentaje = porcentaje;
  }
}
